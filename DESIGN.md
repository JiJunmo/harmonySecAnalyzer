# harmonySecAnalyzer 设计

## 1. 工作流

```text
JSON5 多模块项目事实 + Atlas 全量索引
  -> 脚本生成组件分析单元与任务
  -> 每个组件独立完成语义分析，落盘本地操作组与组件连接
  -> 脚本连接外部入口、组件和敏感操作
  -> 六维验证读取连接后的完整证据
  -> 根因 Finding + 漏洞证据路径
  -> 确定性报告
```

系统覆盖外部入口和实际可达的安全相关操作，不枚举所有代码路径，也不预先构造入口与敏感 API 的笛卡尔积。

## 2. 组件边界

| 组件 | 负责 | 不负责 |
|---|---|---|
| Project Profiler | 确定性解析 JSON5/Manifest，输出构建模块、HAP/HSP/HAR 类型、本地模块依赖、组件和入口候选 | 源码语义、跨模块调用可达性、漏洞判断 |
| Atlas Indexer | 审计前建立完整源码索引 | 项目配置解析、漏洞判断 |
| Analysis Unit Builder（Python） | 按 `component_id` 归并 Manifest 候选，按 module 归并动态入口候选，并直接创建组件任务 | 判断候选是否形成真实源码入口 |
| Component Semantic Analyzer | 使用 Atlas 分析一个组件内的输入、数据传递、实际操作和防护事实；到组件边界时记录参数、身份和权限变化 | 继续分析下游组件、漏洞分类、六维判断、生成报告 |
| Component Correlator（Python） | 连接各组件的参数映射与身份权限变化，只保留从真实外部入口到敏感操作的最短可控链 | 理解源码、猜测缺失连接、创建 AI 路径任务 |
| Exploitability Validator | 根据连接后的证据执行六维判断和反证分析，并可定点读取源码或使用 Atlas 核实 | 重新发现路径、补写语义事实 |
| Audit Runtime | 任务、事务、协议校验、确定性 ID、根因归并、报告准入 | 理解源码语义 |
| Atlas MCP | 符号、调用、变量传播和影响证据 | 审计状态管理 |

## 3. 分析单位

分析单元代表一个待检查的 Ability/ExtensionAbility 组件，或一个 module 级动态入口调查范围。模块由根构建配置确定，以稳定 `module_id` 区分同名模块；未列入构建或属于测试源集的组件不生成任务。每个构建组件都有一个 `component_scope` 候选，使未对外暴露但可能被其他组件调用的组件也只分析一次；它不代表该组件可被外部直接访问。JSON5 中的不同触发渠道作为候选 facets 输入，同一组件只生成一个组件分析任务。

每个实际进入审计范围的分析单元最多创建一个语义任务，完成组件输入确认、本地操作收集、等价操作归并、组件连接和覆盖记录。全量模式从全部组件开始；能力和组件模式从命令指定的范围开始，并沿已证明保持控制性的 `component_calls` 扩展下游组件。没有新的组件后，Python 连接完整参数链和身份权限链。未导出的组件仍可作为下游节点，但不能成为外部根入口。只有起始外部入口可达的本地操作组和跨组件操作组进入六维验证；普通代码分支和组件连接不会生成路径任务。

Operation Group 是语义分析的核心单位。只有以下任一项真实不同才拆组：

1. 敏感操作源码位置。
2. 进入操作的关键受控参数集合。
其余代码分支合并到同一组的 `branches` 中。跨组件路径中，原始身份是否保留、下游实际观察主体、使用的权限或 安全检查 约束对象不同，属于安全语义变化，关联器会分别保留；相同身份权限语义仍归并。观察到的防护代码和业务上下文作为事实保存，不在语义阶段判断其有效性或是否越界。每组保存一条从入口到操作和影响的最短证据链。

## 4. 安全判断

每个操作组先检查业务意图、有效防护和其他反证，再逐项记录六维结论：外部可达、关键参数可控、到达敏感操作、防护缺失或可绕过、安全边界被突破、存在具体影响。

DoS 使用同一流程，不创建独立任务或规则引擎。语义分析对外部可触发的崩溃、无界或输入放大的资源消耗、可重复的队列/IPC/事件耗尽记录结构化可用性事实。六维验证另外核实单次致命或可重复放大、实质不可用影响、输入/频率上限、异常处理和隔离恢复。只有失败或放大可由外部调用者稳定触发、造成实质可用性损失且没有有效限制或隔离时，才允许确认漏洞。Native/NAPI 导致的崩溃和内存安全 DoS 仍不在当前范围内。

跨组件组还必须判断原始外部主体、直接调用组件和下游观察主体的关系。下游仅信任中间组件的白名单不是自动有效防护：若外部主体可控制中间组件、中间组件替换了原始身份并使用自身权限访问下游，则按代理借权验证；若原始身份被可靠保留并与下游策略绑定，则该身份校验可作为反证。

六维是统一判断框架，不要求每个操作组六项都满足。只有 `confirmed_vulnerability` 必须六项全真、没有有效反证，并包含 operation 和 effect 事实。有效防护为 `protected_exposure`；公开业务且未越界为 `benign_business_flow`；现实风险缺关键证据为 `residual_risk`；证据不足为 `insufficient_evidence`。

能力注册表只保留能力标识、名称、领域、状态和适用入口类型，用于审计范围选择。六维判断只依赖已落盘语义事实和固定验证契约。

Finding 根因只使用操作位置、关键受控参数和安全边界。入口别名、普通分支和能力 ID 不制造新根因。只有已确认漏洞和残余风险生成报告中的攻击路径；有效防护和正常业务保留为覆盖证明。

## 5. 调度与状态

任务流水线为：

```text
component_semantic_analysis -> deterministic_component_correlation -> exploitability_validation
```

Python 初始化完整组件目录，并根据命令模式创建起始语义任务。每轮语义提交通过后只原子落库；当前语义任务全部结束时，脚本先检查保持控制性的组件传递，为尚未分析的目标组件补充任务。范围不再扩展后，关联脚本只从命令确定且已确认的外部根入口出发遍历组件；循环通过状态去重终止，每个根入口最多处理固定数量状态，只为命中敏感操作的最短链生成跨组件操作组。然后按根入口创建六维验证任务。

`run.db` 是唯一可变事实源。Agent 只写私有 submission；运行时在每批任务返回后统一检查文件，校验 JSON Schema、证据引用、操作事实、组身份和六维一致性，并在事务中落库。编排 Agent 不解析 worker 回复，也不逐项决定提交或失败。缺失或无效结果最多尝试三次，耗尽后只记录当前任务缺口；固定的模型服务或 MCP 故障不由项目增加复杂兜底。

HTML 报告是 `run.db` 的动态只读视图：初始化、任务领取和批量收敛后由运行时原子重建，用户刷新浏览器即可查看最新状态。Markdown、完整 JSON 导出和哈希快照只在最终报告阶段生成，动态展示不参与任务状态推进。

报告准入要求：没有 queued/running 任务。未完成任务、缺少语义分析的入口和缺少六维验证的操作组进入覆盖缺口，但不阻止报告生成。入口确认状态保存在组件任务的 coverage 中。旧版数据库不迁移，Schema 版本不一致时明确拒绝恢复。

## 6. 运行产物

```text
<run_dir>/
  run.db
  session.json
  project/project_model.json
  tasks/<task>.json
  tasks/<task>.result.json
  exports/entries.json
  exports/semantic_analyses.json
  exports/component_calls.json
  exports/component_graph.json
  exports/operation_groups.json
  exports/validation_results.json
  exports/evidence_paths.json
  exports/attack_matrix.json
  exports/tasks.json
  findings.json
  report_model.json
  report.md
  report.html
  report_snapshot.json
```

JSON、Markdown 和 HTML 全部从 SQLite 重建。HTML 提供概览、组件审计、攻击路径、项目结构、覆盖与缺口五个中文视图。攻击路径只展示需要处置的 Finding 证据链；组件审计展示所有已识别组件，包括未发现漏洞、已有有效防护、正常业务、入口排除和证据不足的结果，并提供组件功能、防护事实、已检查范围与人工复核提示。

## 7. 扩展原则

新增审计能力时，能力注册表只负责审计范围和入口适用性。需要改变自动判定的规则必须明确进入语义事实契约或六维验证契约。Native/NAPI 当前不在范围内。
