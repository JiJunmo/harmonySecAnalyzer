# harmonySecAnalyzer 设计

## 1. 工作流

```text
JSON5 多模块项目事实 + Atlas 全量索引
  -> 脚本生成组件探索单元与持久任务
  -> Agent 按安全语义断点分轮探索，一步连续分析多个普通函数
  -> 脚本从已闭合断点生成唯一组件语义结果
  -> 脚本连接外部入口、组件和敏感操作
  -> 六维验证读取连接后的完整证据
  -> 根因 Finding + 漏洞证据路径
  -> 确定性报告
```

系统覆盖外部入口和实际可达的安全相关操作，不枚举所有代码路径，也不预先构造入口与敏感 API 的笛卡尔积。

## 2. 组件边界

| 组件 | 负责 | 不负责 |
|---|---|---|
| Project Profiler | 确定性解析 JSON5/Manifest，输出构建模块、HAP/HSP/HAR 类型、本地模块依赖、组件和入口候选 | 源码语义、跨模块调用可达性、漏洞判断 |
| Atlas Indexer | 审计前建立完整源码索引 | 项目配置解析、漏洞判断 |
| Analysis Unit Builder（Python） | 按 `component_id` 归并 Manifest 候选，创建组件探索状态和持久语义任务 | 判断候选是否形成真实源码入口、预先展开函数图 |
| Component Semantic Analyzer | 以完整路径为自然边界领取安全语义断点，优先使用 Atlas 定位关系，并用调用点和绑定/分派点源码证据补全动态调用缺边；当前路径闭合后可继续下一条短路径，累计函数达到保护值时保存证据并换新上下文 | 无锚点扫描全仓、凭名称猜测动态目标、直接修改中央状态、手写最终组件结果、继续分析下游 Manifest 组件、漏洞分类、六维判断、生成报告 |
| Semantic Exploration Runtime（Python） | 深度优先领取节点、按安全状态去重、校验步骤结果、保存断点、接续轮次，并从全部闭合节点生成唯一组件语义结果 | 读取 Atlas 私有数据库、理解源码语义、替 Agent 选择调用分支 |
| Component Correlator（Python） | 连接各组件的参数映射与身份权限变化，只保留从真实外部入口到敏感操作的最短可控链 | 理解源码、猜测缺失连接、创建 AI 路径任务 |
| Exploitability Validator | 根据连接后的证据执行六维判断和反证分析，并可读取当前操作组定性所需的完整调用链源码 | 发现新的独立操作、另建路径、补写语义事实 |
| Result Writer（Python） | 将六维验证和 PoC 草稿规范化，补齐任务固定字段、去重并过滤越界证据引用，执行严格契约校验并在同一事务中正式落库 | 编造安全理由、影响链或 PoC 内容，放行实质性结论冲突 |
| Audit Runtime | 任务、事务、协议校验、确定性 ID、根因归并、报告准入 | 理解源码语义 |
| Atlas MCP | 提供优先使用的符号、调用、变量传播和影响索引 | 保证动态调用图完整、审计状态管理 |

## 3. 分析单位

分析单元代表一个待检查的 Ability/ExtensionAbility 组件。组件边界由 Manifest 组件身份决定，不由源码目录、构建模块、依赖包或类继承关系决定；继承方法、覆写与 `super` 调用、helper、异步回调以及索引中可读取的依赖实现都属于当前组件调用链。只有通过组件通信机制进入另一个 Manifest 组件时才形成 `component_calls`。模块由根构建配置确定，以稳定 `module_id` 区分同名模块；未列入构建或属于测试源集的组件不生成任务。每个构建组件都有一个 `component_scope` 候选，使未对外暴露但可能被其他组件调用的组件也只分析一次；它不代表该组件可被外部直接访问。JSON5 中的不同触发渠道作为候选 facets 输入，同一组件只生成一个组件分析任务。Project Profiler 不为每个模块创建 CommonEvent 调查任务，相关代码只在所属组件的语义范围内处理。依赖实现不在 Atlas 索引中时记录具体覆盖缺口，不得视为没有行为。

每个实际进入审计范围的分析单元只有一个持久语义任务记录和一个 `component_exploration`，但可以按上下文容量连续派发多轮短任务。每轮 Agent 优先走完当前路径，路径较短且函数预算尚有余量时继续下一条待分析路径。普通 helper、继承实现和连续调用在同一步内分析，不再逐函数落盘；长路径达到轮次函数保护值时保留当前证据和后续断点，下一轮续上，不作为覆盖缺口。组件断点总数和总轮数仅作最终异常保护，触发时生成可见覆盖缺口。只有所有已发现分支均已完成、停止或形成明确缺口时，脚本才生成最终组件语义结果。

Agent 只声明事实和独立的暂停请求，不填写步骤处理状态。`pause_requested=true` 表示保存当前分段后换上下文；已知待分析目标写入 successors，`stop_reason=null`，有正常边界原因则只记录目标不展开，不再同时填写 decision。已排队的兄弟分支不用重复声明。next 结束本轮，finish 根据实际待办数量重新排队同一 task_id 或完成组件。暂停请求本身不会制造任务或覆盖缺口。

真实解析缺口唯一写在结构化 `gaps` 中，每项包含 target、reason 和 evidence；facts 不再有 gap 类型，停止原因不再有 unresolved。Atlas 未解析表达式是工具观察，必须由源码补全或对应一个 gap 目标；最终覆盖只从已确认缺口、入口不确定性和脚本保护结果推导。未知目标无需创建虚构 successor。正常返回、缺口、其他待办和暂停请求可以同时存在。

脚本将所有已提交分段统一记为节点 `completed`，它只表示处理完成，不表示无缺口。`stopped` 仅用于尚未展开的正常边界目标，`gap` 仅用于脚本总量保护截断的未处理节点。后续分支再次到达已提交节点时复用已有结果，不因停止原因或缺口重复派发；只有未展开的边界目标可能被新关系重新打开。record 返回 `node_status`，finish 返回 `task_status`，组件 complete/partial 则是独立的覆盖结论。

分段完成不能替代整个函数完成。每步必填 `resume`：null 表示当前范围已检查完，其他去向均已登记；当前函数内部未完时保存源码位置、剩余工作和安全状态。脚本在现有节点表中保存续跑位置，并用相同函数与安全状态加该位置创建后续分段，仍由原任务领取。已保存分段可复用，但其后续分段未完成时组件不能完成；普通递归调用仍按原有函数身份去重。它不新增 Agent、脚本、任务类型或数据库。

入口判断只保留一份最新值，但允许随证据更新。入口发现必须给出初判，后续函数分析可用同一个 `entry_assessment` 携带新定位证据提交完整组件判断；next 返回当前值供各轮继续使用。字段唯一不等于只能写一次。排除外部触发不代表排除内部输入；与已记录语义事实矛盾的组件排除在提交时拒绝，不拖到报告阶段失败。

节点安全状态中的检查引用由源码位置、检查对象和校验属性构成，脚本据此生成稳定身份。新增引用必须对应带证据的检查，继承引用从当前节点取得。控制和身份标签按明确选择顺序互斥使用：存在显式约束但仍受控时为 constrained；只能可靠获得中间调用方身份时为 replaced_by_caller；调用身份 API 检查始终标为 immediate_caller，不因其碰巧也是原始主体而切换。它们不代替六维判断。此步骤契约通过运行数据版本拒绝旧格式续跑，更新后需新建审计；不新增数据库或兼容流程。

组件输入状态与外部入口状态独立记录，只有源码确认的外部候选可成为根入口。`form` 类型 ExtensionAbility 的 `exported=true` 只表示卡片框架可见，不生成普通应用直接调用候选；组件及其卡片生命周期仍参与语义分析，并可由已证明的上游组件调用进入后续验证。全量模式和组件级能力模式从全部组件开始；组件过滤模式从指定组件开始，并沿已证明保持调用触发控制或数据控制的 `component_calls` 扩展下游组件。能力的 `entry_types` 只提示常见入口，不参与组件排除。没有新的组件后，Python 分别连接调用触发链、参数链和身份权限链。未导出的组件仍可作为下游节点，但不能成为外部根入口。只有真实外部入口可达的本地操作组和跨组件操作组进入六维验证；普通代码分支和组件连接不会生成路径任务。

Operation Group 是语义分析的核心单位。能力、操作源码位置、关键受控参数、调用主体、业务用途、直接效果或适用安全检查中任一安全语义真实不同时拆组；其余普通代码分支合并到同一组的 `branches` 中。跨组件路径中，原始身份是否保留、下游实际观察主体、使用的权限或安全检查约束对象不同，关联器会分别保留；相同身份权限语义仍归并。观察到的防护代码和业务上下文作为事实保存，不在语义阶段判断其有效性或是否越界。每组保存一条从入口到操作的最短证据链，但任何会改变后续安全结论的分支、参数转换、身份变化和安全检查都不得省略。

## 4. 安全判断

每个操作组先检查业务意图、有效防护和其他反证，再逐项记录六维结论：外部可达、操作触发或安全关键参数受控、到达敏感操作、防护缺失或可绕过、安全边界被突破、存在具体影响。每一维使用 `true/false/unknown` 三态并绑定理由、证据等级和证据引用：`true` 必须有成立证据，`false` 必须有反向证据，缺少关键证明只能使用 `unknown`；`direct/derived/hypothesis` 分别表示源码直接证明、由已证实事实明确推导和仍依赖假设。Agent 提示词负责说明判定标准，JSON Schema 限定字段和枚举，运行时校验器拒绝证据不足或状态互相冲突的结果。

DoS 使用同一流程，不创建独立任务或规则引擎。语义分析对外部可触发的崩溃、无界或输入放大的资源消耗、可重复的队列/IPC/事件耗尽记录结构化可用性事实。六维验证另外核实单次致命或可重复放大、实质不可用影响、输入/频率上限、异常处理和隔离恢复。只有失败或放大可由外部调用者稳定触发、造成实质可用性损失且没有有效限制或隔离时，才允许确认漏洞。Native/NAPI 导致的崩溃和内存安全 DoS 仍不在当前范围内。

跨组件组还必须判断原始外部主体、直接调用组件和下游观察主体的关系。下游仅信任中间组件的白名单不是自动有效防护：若外部主体可控制中间组件、中间组件替换了原始身份并使用自身权限访问下游，则按代理借权验证；若原始身份被可靠保留并与下游策略绑定，则该身份校验可作为反证。

六维是统一判断框架，不要求每个操作组六项都满足。语义 facts 只保存源码事实，字段名、注释或业务词义产生的效果判断进入 `effect_hypotheses` 并列明缺失证明。只有 `confirmed_vulnerability` 必须六项全真、没有有效反证，并由本轮验证新增源码证据证明“受控触发或受控值使用 → 安全行为变化 → 受保护操作 → 具体影响”的完整 `effect_chain`。有效防护为 `protected_exposure`；外部可达、操作控制或操作可达被明确反证时为 `no_exploitable_path`；公开业务且未越界为 `benign_business_flow`；前三项基础路径成立但后续缺关键证据为 `residual_risk`；前三项本身存在未知为 `insufficient_evidence`。

组件语义阶段用两个互不替代的状态消除入口歧义：`entry_status` 判断组件是否存在真实可执行输入，内部上游调用成立也可为 `confirmed`；`external_entry_status` 只判断非 `component_scope` 候选是否形成真实外部入口，并用 `confirmed_external_candidate_ids` 指明证据对应的候选。只有后者为 `confirmed` 的组件才可成为跨组件连接和本地六维验证的起点。

PoC Agent 不输出可信度状态。运行时接受产物后标记 `generated_unverified`，明确表示仅通过结构、证据引用和静态触发检查，尚未编译或执行；未来的编译和设备验证阶段才可推进为 `build_verified` 或 `device_verified`。PoC 失败不构成审计覆盖缺口，但在对应 Finding 中显示 `generation_failed`。

能力注册表只保留能力标识、名称、领域、状态和常见入口类型。能力 ID 选择审计内容，入口类型只作为分析优先提示；六维判断只依赖已落盘语义事实和固定验证契约。

Finding 根因只使用操作位置、关键受控参数和安全边界。入口别名、普通分支和能力 ID 不制造新根因。只有已确认漏洞和残余风险生成报告中的攻击路径；有效防护和正常业务保留为覆盖证明。

## 5. 调度与状态

任务流水线为：

```text
component_semantic_analysis -> deterministic_component_correlation -> exploitability_validation -> poc_generation
```

Python 初始化完整组件目录，并根据命令模式创建起始探索状态和持久语义任务。同一任务可被多轮领取；一轮成功但仍有待分析节点时重新排队且清零本轮重试次数，节点进度不回滚。所有节点闭合后，运行时确定性生成并落库最终语义结果。当前语义任务全部结束时，脚本先检查保持控制性的组件传递，为尚未分析的目标组件补充探索状态和任务。范围不再扩展后，关联脚本只从命令确定且已确认的外部根入口出发遍历组件；循环通过状态去重终止，每个根入口最多处理固定数量状态，只为命中敏感操作的最短链生成跨组件操作组。然后按根入口创建六维验证任务。

`run.db` 是唯一可变事实源。语义 Agent 只写当前节点步骤文件并调用受控探索命令；`explore-record` 即时校验节点，`explore-finish` 在同一事务中重新排队或生成、校验并落库最终组件结果。六维验证与 PoC Agent 只写任务私有 draft，并调用 `audit_orchestrator.py task-submit`；Result Writer 补齐固定 ID 和缺省字段、规范证据引用，用严格 Schema 与业务不变量验收，并在同一事务中写入正式结果、完成任务。校验错误在子任务上下文仍存在时以命令成功、`accepted=false` 直接返回，不消耗调度重试。编排 Agent 不解析 worker 回复；每批结束后的 `reconcile-batch` 只回收仍停在 `running` 的中止任务。此类任务最多尝试三次，耗尽后记录覆盖缺口；固定的模型服务或 MCP 故障不由项目增加复杂兜底。

HTML 报告是 `run.db` 的动态只读视图：初始化、任务领取、任务即时提交和异常回收后由运行时原子重建，用户刷新浏览器即可查看最新状态，并可查看每个组件的探索轮次、节点数量、停止原因和覆盖缺口。Markdown、完整 JSON 导出和哈希快照只在最终报告阶段生成，动态展示不参与任务状态推进。

报告准入要求：没有 queued/running 任务。未完成任务、缺少语义分析的入口和缺少六维验证的操作组进入覆盖缺口，但不阻止报告生成。入口确认状态保存在组件任务的 coverage 中。旧版数据库不迁移，Schema 版本不一致时明确拒绝恢复。

### 增量分析

增量分析不创建第二套审计流程。上次成功的无过滤全量或增量运行在 `reports/incremental-baseline/` 保存项目模型、可追踪文件内容哈希和已校验的组件语义结果。Git 项目额外记录基线提交，用于汇总两次审计之间的累计提交变化和重命名；文件哈希仍是是否变化的统一事实，因此同时覆盖 Git 工作区和非 Git 项目。

脚本对比新旧项目模型识别新增、删除和定义改变的入口，将变化文件映射到所属模块，再沿反向模块依赖扩展影响范围。模块外共享源码或顶层构建/应用配置变化按全局影响处理；历史组件调用的目标入口被删除时，调用方同样失效并重新分析。这一保守的模块级规划能覆盖入口不变但敏感操作、防护或公共 helper 更新的情况。

受影响组件执行原有完整语义分析；未受影响组件的历史语义结果必须重新通过当前 Schema、证据引用和组件目录校验后才能导入新 `run.db`。运行时使用当前完整语义状态重新计算组件连接；若同一入口下的操作组集合及全部安全语义指纹与基线一致，则复用完整六维验证结论，否则重新验证该入口。只有运行无 exhausted 任务、无缺失语义或验证结果时才推进基线；失败或部分审计不改变旧基线。
基线同时保存能力注册表、组件语义 Schema 和语义 Agent 的审计契约指纹；指纹变化时禁止复用并要求重新全量审计。
最终报告用稳定 Finding ID 对比基线与当前结果，区分新增、结论变化、已消失和未变的风险路径；该对比只用于报告，不反向影响审计任务调度。

## 6. 运行产物

```text
<run_dir>/
  run.db
  session.json
  project/project_model.json
  tasks/<task>.json
  tasks/<task>.attempt-<n>.draft.json      # Agent 临时草稿，规范化成功后删除
  tasks/<task>.result.json
  exports/entries.json
  exports/exploration_graph.json
  exports/semantic_analyses.json
  exports/component_calls.json
  exports/component_graph.json
  exports/operation_groups.json
  exports/validation_results.json
  exports/evidence_paths.json
  exports/attack_matrix.json
  exports/tasks.json
  findings.json
  report_model.json
  report.md
  report.html
  report_snapshot.json
```

JSON、Markdown 和 HTML 全部从 SQLite 重建。HTML 提供概览、组件审计、攻击路径、项目结构、覆盖与缺口五个中文视图。攻击路径只展示需要处置的 Finding 证据链；组件审计展示所有已识别组件，包括未发现漏洞、已有有效防护、正常业务、入口排除和证据不足的结果，并提供组件功能、防护事实、已检查范围与人工复核提示。

## 7. 扩展原则

新增审计能力时，能力注册表只负责审计范围和入口适用性。需要改变自动判定的规则必须明确进入语义事实契约或六维验证契约。Native/NAPI 当前不在范围内。
