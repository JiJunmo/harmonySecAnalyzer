# v3.2 已实现系统架构

更新时间：2026-08-03

```mermaid
flowchart TB
    User["本机用户"] --> Web["Web Shell"]
    User --> CLI["Generic CLI"]

    subgraph Gateway["单机 Agent Gateway"]
        Server["HTTP / SSE / Auth / Static Assets"]
        AssistantService["AssistantSessionService"]
        PluginHost["PluginHostService"]
        State["LocalGatewayState"]
    end

    Web --> Server
    CLI --> PluginHost
    Server --> AssistantService
    Server --> PluginHost
    Server --> State
    AssistantService --> State
    PluginHost --> State

    subgraph Core["通用能力骨架"]
        PiFactory["PiSessionFactory"]
        MCP["MCP Manager"]
        Subagents["Generic Subagent Runtime"]
        Contract["Plugin Contract"]
        Pool["Rolling Agent Pool"]
        GraphAdapter["LangGraph Adapter"]
    end

    AssistantService --> PiFactory
    AssistantService --> MCP
    AssistantService --> Subagents
    PluginHost --> Contract
    PiFactory --> Pi["Pi Coding Agent<br/>Model · Auth · Session · Tool · Skill"]

    subgraph Harmony["Harmony White-box Audit Plugin"]
        Adapter["Plugin Adapter<br/>CLI · Web · Run · Artifact"]
        Profiler["Project Profiler"]
        AuditGraph["LangGraph Audit State Machine"]
        HarmonyPool["Five-slot Scheduling Policy"]
        Semantic["Path Discovery Agents"]
        Validation["Six-dimensional Validation Agents"]
        Correlation["Deterministic Correlation"]
        Store[("run.db")]
        Reports["JSON · Markdown · HTML · Matrix"]
    end

    Contract --> Adapter
    Adapter --> Profiler
    Adapter --> AuditGraph
    AuditGraph --> GraphAdapter
    AuditGraph --> HarmonyPool
    HarmonyPool --> Pool
    HarmonyPool --> Semantic
    HarmonyPool --> Validation
    Semantic --> Atlas["Atlas MCP"]
    Validation --> Atlas
    Semantic --> PiFactory
    Validation --> PiFactory
    Semantic --> Store
    Correlation --> Store
    Validation --> Store
    Store --> Reports
```

关键边界：

- 内置对话助手与领域插件在产品入口上平级，但对话是平台内置能力，不需要经过 Plugin Host；
- Plugin Host 只处理通用生命周期，Harmony 的页面、任务和报告语义全部位于插件包；
- Pi 负责单 Agent 的模型—工具循环，LangGraph 负责插件的多 Agent 流程状态；
- 平台 `gateway.db` 是本地 Job 索引，Harmony `run.db` 才是审计事实源；
- “最大五槽”属于 Harmony 调度策略，Core 只提供可配置的滚动池算法。
