# v3.2 能力状态与后续增强

更新时间：2026-08-03

## 当前结论

v3.2 已完成平台迁移、Harmony 审计插件接入、全量/增量审计和单机可靠性建设，达到单用户本地网关的生产可用基线。当前不存在阻塞本地使用的已知架构或主链路功能缺口。

## 已完成能力

| 能力域 | 状态 | 事实来源 |
|---|---|---|
| 通用对话助手 | 已完成 | Pi Session、模型/认证、工具、Skill、MCP、历史会话 |
| Plugin Contract 与 Host | 已完成 | 动态发现、激活、Run、事件、动作、执行过程、Artifact、CLI/Web contribution |
| 通用子 Agent | 已完成 | 隔离 Session、并发、取消、过程展示和本地状态恢复 |
| Harmony Project Model | 已完成 | JSON5/Manifest、模块、组件、入口、依赖和项目级上下文 |
| 全量审计 | 已完成 | 路径发现、确定性关联、六维验证、PoC 生成、Finding 和覆盖缺口 |
| 审计能力目录 | 已完成 | 原 6 项 planned 与 2 项 deferred 已进入组件或项目级分析 |
| 增量审计 | 已完成 | 内容哈希、影响计划、事实重验收、验证复用和风险变化 |
| 五槽任务运行 | 已完成 | 滚动补槽、三次尝试、单任务失败隔离和状态恢复 |
| 报告 | 已完成 | JSON、详细 Markdown、详细 HTML 和 Attack Matrix |
| 本地可靠性 | 已完成 | gateway SQLite、历史发现、重启中断语义、保留清理、日志和诊断 |

历史里程碑的验收记录已从工作区移除，追溯见 git 历史。Harmony 与 v3.1 的细粒度比较见 [V31-PARITY.md](packages/harmony-audit/V31-PARITY.md)。

## 非阻塞增强项

| 方向 | 当前判断 | 建议触发条件 |
|---|---|---|
| 固定跨版本快照验收 | 延后，不影响当前使用 | 审计规则大规模调整或发布稳定版前 |
| v3.1 同名细粒度导出集合 | 兼容性增强 | 外部系统明确依赖旧文件名时 |
| 大型项目性能压测与资源调优 | 生产强化 | 出现真实大型项目耗时或内存瓶颈时 |
| 故障注入、备份与迁移工具 | 运维强化 | 审计数据需要长期归档或跨机器迁移时 |
| 指标、告警和集中日志 | 运维强化 | 网关需要无人值守长期运行时 |
| 更多审计类型插件 | 产品演进 | 新领域需求确定后，按 Plugin Contract 独立实现 |

## 明确暂不实现

当前产品定位是本机单用户网关，因此以下能力不是 v3.2 缺口：

- 公网直接暴露和内置 HTTPS；
- 多用户身份、角色、租户和审计授权；
- 集中数据库、分布式队列和多节点 Worker；
- 面向公网服务的限流、计费和租户隔离。

若部署模型从“单机网关”变为“远程多人服务”，应作为新的架构阶段整体设计，而不是继续在当前代码中零散补丁。

## 后续优先级原则

1. 优先依据真实项目使用反馈修正审计准确率和报告表达；
2. 新领域审计能力以独立插件实现，不修改平台核心；
3. 只有出现明确运维需求时才增加对应生产强化；
4. 模型、认证、Session、基础工具和通用 Skill 能力继续优先复用 Pi；
5. 保持 `pnpm check`、`pnpm test`、`pnpm build` 为每次变更的最低门槛。
